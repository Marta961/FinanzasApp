<UserControl x:Class="Finanzas1.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="12">

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Refrescar" Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
            <Button Content="Añadir" Command="{Binding AddCommand}" Margin="0,0,8,0"/>
            <Button Content="Actualizar" Command="{Binding UpdateCommand}" Margin="0,0,8,0"/>
            <Button Content="Borrar" Command="{Binding DeleteCommand}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Transactions}"
                  SelectedItem="{Binding Selected}"
                  AutoGenerateColumns="False"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Date, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Importe" Binding="{Binding Amount}" Width="*"/>
                <DataGridTextColumn Header="Categorías" Binding="{Binding Category}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</UserControl>


